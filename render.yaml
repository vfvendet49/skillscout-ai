services:
  - type: web
    name: skillscout-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python app/db_init.py || echo "Database init completed or skipped"
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        scope: run
        value: postgresql://user:isom599db@isom599aws.c18k2ewikpxy.us-east-2.rds.amazonaws.com:5432/jobfinder
